apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloudnative-pg-cluster
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/chik4ge-homelab/homelab-applications
    targetRevision: main
    path: manifests/platform/services/data-platform/cloudnative-pg-cluster
  destination:
    namespace: cnpg-database
    server: {{ .Values.spec.destination.server }}
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
