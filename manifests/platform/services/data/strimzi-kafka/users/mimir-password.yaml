apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mimir-password
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: 1password-connect
    kind: ClusterSecretStore
  data:
    - secretKey: password
      remoteRef:
        key: "Kafka mimir User"
        property: password
