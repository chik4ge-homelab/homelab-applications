apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-mcp
  labels:
    app.kubernetes.io/name: grafana-mcp
spec:
  ingressClassName: cloudflare-tunnel
  rules:
    - host: grafana-mcp.chik4ge.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana-mcp
                port:
                  number: 8000
