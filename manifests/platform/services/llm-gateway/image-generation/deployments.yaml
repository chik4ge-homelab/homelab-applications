apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-generation-api
  labels:
    app.kubernetes.io/name: image-generation-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: image-generation-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: image-generation-api
    spec:
      serviceAccountName: image-generation-api
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: ghcr.io/chik4ge-homelab/image-generation-gateway@sha256:d7c22f88d4e1f5a02b56943b4c0b0856de30baee99a053f9b07d60c9a1f7cf8a
          imagePullPolicy: IfNotPresent
          args:
            - api
          env:
            - name: IMAGE_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: ARGOCD_APPLICATION_NAME
              value: llm-gateway
            - name: LLM_GATEWAY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-gateway-api-key
                  key: api-key
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
  strategy:
    type: RollingUpdate
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-generation-controller
  labels:
    app.kubernetes.io/name: image-generation-controller
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: image-generation-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: image-generation-controller
    spec:
      serviceAccountName: image-generation-controller
      securityContext:
        runAsNonRoot: true
        runAsUser: 100
        runAsGroup: 101
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: controller
          image: ghcr.io/chik4ge-homelab/image-generation-gateway@sha256:d7c22f88d4e1f5a02b56943b4c0b0856de30baee99a053f9b07d60c9a1f7cf8a
          imagePullPolicy: IfNotPresent
          args:
            - controller
          env:
            - name: IMAGE_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: ARGOCD_APPLICATION_NAME
              value: llm-gateway
            - name: ARTIFACT_ENDPOINT
              value: http://external-rgw-backend.rook-ceph.svc.cluster.local:7480
            - name: BUCKET_NAME
              value: image-generation-artifacts
            - name: LLM_RESTORE_TIMEOUT_SECONDS
              value: "300"
          envFrom:
            - secretRef:
                name: image-generation-artifacts
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
