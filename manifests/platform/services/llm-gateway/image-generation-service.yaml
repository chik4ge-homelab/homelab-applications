apiVersion: v1
kind: Service
metadata:
  name: image-generation-api
  labels:
    app.kubernetes.io/name: image-generation-api
spec:
  selector:
    app.kubernetes.io/name: image-generation-api
  ports:
    - name: http
      port: 8080
      targetPort: http
