apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - externalsecret.yaml
  - httproute.yaml
  - pvc.yaml
  - service.yaml
configMapGenerator:
  - name: llama-cpp-chat-template
    files:
      - chat_template.jinja
  - name: llama-cpp-gateway-proxy
    files:
      - default.conf=gateway-proxy.conf
