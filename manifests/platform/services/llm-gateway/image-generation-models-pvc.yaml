apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-generation-models
  labels:
    app.kubernetes.io/name: image-generation
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  volumeMode: Filesystem
  resources:
    requests:
      storage: 20Gi
