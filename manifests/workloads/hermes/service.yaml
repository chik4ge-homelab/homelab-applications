apiVersion: v1
kind: Service
metadata:
  name: hermes-gateway
  labels:
    app.kubernetes.io/name: hermes
spec:
  selector:
    app.kubernetes.io/name: hermes
  ports:
  - name: gateway
    port: 8642
    targetPort: gateway
